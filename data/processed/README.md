# Processed data

Files here are **generated** by Phase 2+ scripts (cleaned rectangles ready for feature engineering).

Default output from `python scripts/run_phase2_eda.py`:

- `clean_accidents.csv` — same columns as raw, after strip / type checks / row drops documented in `utils.clean_accidents_df`.
